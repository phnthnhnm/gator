# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

release:
  skip_upload: true

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^refactor:"
      - "^style:"
